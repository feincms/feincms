This is an assorted list of stuff.


Internal variables FeinCMS uses:

_feincms_content_types
_feincms_content_types_with_process
_feincms_content_types_with_finalize


ApplicationContent parameters

    * urls
    * admin_fields
    * path_mapper
    * view_wrapper



The stages when processing a request

Now:

- Determination of Page instance
(- Collect application contents and process them)
- Render template
    - Collect and render content types
- Return response


Reworked:

- Determine Page instance
- Collect content type objects and render them
    - Collect media files etc. in the process
    - If any returns a special object (f.e. a HttpResponse) do something else
- Render template using pre-rendered content types
